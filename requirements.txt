# Web framework
Flask==2.3.3
Flask-Session==0.4.0

# Deployment (WSGI server)
gunicorn==21.2.0

# Resume / JD parsing
mammoth==1.4.2
python-docx==0.8.11
PyPDF2==3.0.1

# Text-to-speech
gTTS==2.3.2

# Data handling for admin reports
pandas==2.2.2

# NLP (optional: if using for scoring/keywords)
spacy==3.7.2

# Environment variable support
python-dotenv==1.0.1

# Optional: For local playback of TTS (not needed on server like Render)
# playsound==1.3.0
